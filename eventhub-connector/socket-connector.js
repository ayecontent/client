// Generated by CoffeeScript 1.7.1
"use strict";
var EventHubConnector, Socket, crypto, errors, events, querystring, socketIO, util, _,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

_ = require("lodash");

crypto = require("crypto");

querystring = require("querystring");

events = require("events");

errors = require("./../errors");

socketIO = require("socket.io-client");

util = require("util");

EventHubConnector = require("./../eventhub-connector");

Socket = (function(_super) {
  __extends(Socket, _super);

  Socket.prototype._RECONNECT_MIN_DELAY = 1000;

  Socket.prototype._RECONNECT_MAX_DELAY = 10000;

  function Socket(args) {
    this.logger = args.logger, this.config = args.config;
    this._connectString = "ws://" + this.config.get("eventhub:address") + ":" + this.config.get("eventhub:port");
    this._clientId = this.config.get("client:id");
    this._signature = this.config.get("client:sign");
  }

  Socket.prototype.initListeners = function() {
    this._socket.on("connect", (function(_this) {
      return function() {
        _this.logger.info("Client '" + _this._clientId + "' connected to EVENT-HUB. " + (_this.logger.printTime("Connection to EVENT-HUB")));
        return _this._delay = _this._RECONNECT_MIN_DELAY;
      };
    })(this));
    this._socket.on("message", (function(_this) {
      return function(message, callback) {
        _this.logger.info("Received EVENT-HUB message: '" + (util.inspect(message, {
          depth: 30
        })) + "'");
        _this.logger.startTime("Message '" + message.id + "' in client");
        return _this.emit("command", message.command, function(err, result) {
          _this.logger.info(_this.logger.printTime("Message '" + message.id + "' in client"));
          if (err != null) {
            err = errors.stringifyError(err);
          }
          return callback(err, result);
        });
      };
    })(this));
    this._socket.on("disconnect", (function(_this) {
      return function() {
        _this.logger.info("Client '" + _this._clientId + "' disconnected from EVENT-HUB");
        return _this.reconnect();
      };
    })(this));
    return this._socket.on("error", (function(_this) {
      return function(err) {
        _this.logger.error("EVENT-HUB connection error: '" + err + "'");
        return _this.reconnect();
      };
    })(this));
  };

  Socket.prototype.start = function() {
    var client, query;
    this.logger.info("Start Socket EVENT-HUB Connector");
    client = this.config.get("client");
    query = querystring.stringify(client);
    this.logger.info("Start connection to EVENT-HUB");
    this.logger.startTime("Connection to EVENT-HUB");
    this._socket = socketIO.connect(this._connectString, {
      reconnect: false,
      query: query
    });
    return this.initListeners();
  };

  Socket.prototype.reconnect = function() {
    this._socket.socket.disconnect();
    if (this._delay == null) {
      this._delay = this._RECONNECT_MIN_DELAY;
    }
    this._delay = this._delay < this._RECONNECT_MAX_DELAY ? this._delay : this._RECONNECT_MAX_DELAY;
    setTimeout((function(_this) {
      return function() {
        _this.logger.info("Start reconnection to EVENT-HUB");
        _this.logger.startTime("Connection to EVENT-HUB");
        return _this._socket.socket.connect();
      };
    })(this), this._delay);
    return this._delay *= 2;
  };

  return Socket;

})(EventHubConnector);

module.exports = Socket;
