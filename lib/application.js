// Generated by CoffeeScript 1.7.1
"use strict";
var Application, Socket, Sync;

Socket = require("./../connector/socket");

Sync = require("./../sync");

Application = (function() {
  function Application(args) {
    this.logger = args.logger, this.config = args.config;
    this.eventHubConnector = new Socket({
      logger: this.logger,
      config: this.config
    });
    this.sync = new Sync({
      logger: this.logger,
      config: this.config
    });
  }

  Application.prototype.configure = function() {
    return this.initListeners();
  };

  Application.prototype.start = function() {
    this.configure();
    this.eventHubConnector.start();
    return this.sync.startAutoSync();
  };

  Application.prototype.initListeners = function() {
    this.eventHubConnector.on("connected", (function(_this) {
      return function(event) {
        return _this.logger.info("connected to event hub");
      };
    })(this));
    return this.eventHubConnector.on("command", (function(_this) {
      return function(command, callback) {
        return _this.sync.pushCommand(command).then(function(result) {
          return callback(null, result);
        })["catch"](function(err) {
          return callback(err);
        }).done();
      };
    })(this));
  };

  return Application;

})();

module.exports = Application;
